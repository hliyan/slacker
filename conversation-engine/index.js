// a conversation engine that responds to text input with text responses

/*
// the main engine object. contains functions + state
const engine = {};

// returns a response of the form {username, text}
const say = async ({username, text}) => {
  // hard coding the initial implementation for test purposes
  if (text === 'hello') {
    return {username, text: `Hi, ${username}`}
  }

  // default greeting
  return {username, text: 'Hello'};
};

module.exports = engine;
*/